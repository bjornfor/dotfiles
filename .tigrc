# ~/.tigrc -- config file for tig (text-mode interface to git)

bind generic J scroll-line-down
bind generic K scroll-line-up
# "show-rev-graph" option is obsolete in tig 2.x (it's the default behaviour)
#set show-rev-graph = yes

## these don't work
#bind generic 1 Home
#bind generic 0 End
